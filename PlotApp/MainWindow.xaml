<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PlotApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PlotApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    
    xmlns:sc="using:ScottPlot.WinUI"
    mc:Ignorable="d"
    Title="PlotApp">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height=".25*"/>
        </Grid.RowDefinitions>
        <MenuBar Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <MenuBarItem Title="Настройки осей">
                <MenuFlyoutItem Text="Название оси X" x:Name="EnterX" Click="EnterX_Click"/>
                <MenuFlyoutItem Text="Название оси Y" x:Name="EnterY" Click="EnterY_Click"/>
            </MenuBarItem>
            <MenuBarItem Title="Легенда" >
                <MenuFlyoutItem Text="Изменить название графика" Click="EnterName_Click" x:Name="EnterName"/>
            </MenuBarItem>
        </MenuBar>
        <controls:DataGrid Grid.Column="0" Grid.Row="1" x:Name="dataGrid" AutoGenerateColumns="True" SelectedIndex="{x:Bind _viewModel.SelInd, Mode=TwoWay}" ItemsSource="{x:Bind _viewModel.Model, Mode=TwoWay}" KeyDown="dataGrid_KeyDown"/>
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center" VerticalAlignment="Top">
                <Button Click="{x:Bind _viewModel.AddRow}" x:Name="PlusBtn">
                    <SymbolIcon Symbol="Add"/>
                </Button>
                <Button Click="Button_Click">
                    <SymbolIcon Symbol="Folder"/>
                </Button>
                <Button Margin="15 0 0 0">
                    <SymbolIcon Symbol="Previous"/>
                </Button>
                <TextBlock Text="1/1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button>
                    <SymbolIcon Symbol="Next"/>
                </Button>
            </StackPanel>
            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Text="{x:Bind _viewModel.Selected, Mode=OneWay}" />
        </Grid>
        <sc:WinUIPlot Grid.Column="1" Grid.Row="1" x:Name="mainplot"/>
        <TextBlock Text="Заполните данные" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" FontSize="20" FontWeight="Light" HorizontalAlignment="Center" Visibility="{x:Bind _viewModel.WarningVisibility, Mode=OneWay}"/>
        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" Spacing="7">
            <CheckBox Content="Интерполяция графика" IsChecked="{x:Bind _viewModel.isSmooth, Mode=TwoWay}" Checked="{x:Bind _viewModel.SetSmooth}" Unchecked="{x:Bind _viewModel.UnsetSmooth}"/>
            <Button Content="Цвет графика">
                <Button.Flyout>
                    <Flyout>
                        <ColorPicker
                            IsColorSliderVisible="True"
                            IsHexInputVisible="False"
                            IsColorChannelTextInputVisible="False"
                            ColorChanged="{x:Bind _viewModel.ChangeColor}"/>
                    </Flyout>
                </Button.Flyout>
            </Button>
            <TextBlock Text="Стиль линии:" VerticalAlignment="Center" Margin="20 0 0 0"/>
            <RadioButton Content="Сплошная" Checked="{x:Bind _viewModel.UndashLine}"/>
            <RadioButton Content="Пунктирная" Checked="{x:Bind _viewModel.DashLine}"/>
        </StackPanel>
    </Grid>
</Window>
